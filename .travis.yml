language: node_js
before_script:
  - npm install
script:
- npm run build
deploy:
  provider: pages
  token: $GITHUB_TOKEN
  cleanup: true
  keep_history: false
  local_dir: public
  on:
    branch: master
